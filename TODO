1. startup splash screen
2. UI - top bar
3. autolaunch app
4. create update page
5. make button inside current app that can switch workspace


# to switch tags
awesome-client 'n = require("awful"); n.tag.viewonly(root.tags()[1])'



# this is the shell script that I need to run
cd ~/projects/smartlift-node-monitor
npm install
typings install
./node_modules/.bin/electron-rebuild
npm run build
electron app ws://zuik.dnsalias.net:8095
